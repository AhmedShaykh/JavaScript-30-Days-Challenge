let persons = ["Junaid", "Ahsan", "Haris"];

for (var index = 0; index < persons.length; index++) {
    const message = persons[index];
    document.write(`<h2>${message} Dinner Tonight is at my Home!.<br/></h2>`);
}